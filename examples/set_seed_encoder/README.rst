About set_seed_encoder example
------------------------------

This example shows how we can use the ``set_seed(uint32_t)``
function defined in the ``linear_block_vector_generator``
layer to change the random seed used to generate symbol ids
in the encoders.
