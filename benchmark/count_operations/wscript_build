#! /usr/bin/env python
# encoding: utf-8

bld.program(
    features = 'cxx test',
    source   = ['main.cpp'],
    target   = 'kodo_count_operations',
    cxxflags = bld.toolchain_cxx_flags(),
    use = ['kodo_includes', 'fifi_includes', 'sak_includes',
           'gtest', 'boost_includes', 'boost_system', 'boost_timer',
           'boost_chrono', 'gauge'])
